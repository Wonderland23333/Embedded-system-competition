#include"data_process.h"
void main()
{
    float temperature,electricity;
    int light;
   // light_dev_init(fd_light);
    while(1);
    {
      //  temperature=  temper();
       // electricity=  electric();
       light = light_intensity();
    }
    exit(0);
}